# NOTE: torch is installed separately in Dockerfile.local (CPU-only wheel).
# For local install without Docker: pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers>=4.35.0,<6.0
sentence-transformers>=2.2.0,<6.0
faiss-cpu>=1.7.4,<2.0
langchain-text-splitters>=1.0.0,<2.0
beautifulsoup4>=4.12.0,<5.0
lxml>=4.9.0,<7.0
ebooklib>=0.18,<1.0
numpy>=1.24.0

# Web interface
fastapi>=0.104.0,<1.0
uvicorn[standard]>=0.24.0,<1.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
